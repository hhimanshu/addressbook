<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<artifactId>addressbook</artifactId>
		<groupId>com.learner</groupId>
		<version>1.0-SNAPSHOT</version>
	</parent>

	<artifactId>integration</artifactId>
	<packaging>war</packaging>
	<name>AddressBook:Integration Tests</name>

	<properties>
		<datasource.jndi>java:/jdbc/addressBook</datasource.jndi>
		<datasource.url>jdbc:h2:file:${project.build.directory}/integrationDB;AUTO_SERVER=TRUE</datasource.url>
		<h2.db.version>1.4.181</h2.db.version>
		<h2.driver>org.h2.Driver</h2.driver>
		<h2.user>sa</h2.user>
		<jackson-mapper-asl.version>1.9.13</jackson-mapper-asl.version>
		<maven-failsafe-plugin.version>2.12</maven-failsafe-plugin.version>
		<maven-war-plugin.version>2.4</maven-war-plugin.version>
		<maven.cargo.version>1.4.8</maven.cargo.version>
		<resteasy-client.version>3.0.8.Final</resteasy-client.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>com.learner</groupId>
			<artifactId>services</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
		</dependency>
		<dependency>
			<groupId>com.learner</groupId>
			<artifactId>persistence</artifactId>
			<version>${project.version}</version>
			<type>test-jar</type>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.hibernate</groupId>
			<artifactId>hibernate-entitymanager</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.jboss.resteasy</groupId>
			<artifactId>resteasy-client</artifactId>
			<version>${resteasy-client.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.codehaus.jackson</groupId>
			<artifactId>jackson-mapper-asl</artifactId>
			<version>${jackson-mapper-asl.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<version>${h2.db.version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<version>${maven-war-plugin.version}</version>
				<configuration>
					<failOnMissingWebXml>false</failOnMissingWebXml>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.cargo</groupId>
				<artifactId>cargo-maven2-plugin</artifactId>
				<version>${maven.cargo.version}</version>
				<executions>
					<execution>
						<id>start-container</id>
						<phase>pre-integration-test</phase>
						<goals>
							<goal>start</goal>
						</goals>
					</execution>
					<execution>
						<id>stop-container</id>
						<phase>post-integration-test</phase>
						<goals>
							<goal>stop</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<container>
						<containerId>wildfly8x</containerId>
						<dependencies combine.children="append">
							<dependency>
								<groupId>com.h2database</groupId>
								<artifactId>h2</artifactId>
							</dependency>
						</dependencies>
					</container>
					<configuration>
						<properties>
							<cargo.servlet.port>9090</cargo.servlet.port>
							<cargo.datasource.datasource.h2>cargo.datasource.driver=${h2.driver}|
                                cargo.datasource.url=${datasource.url}|
                                cargo.datasource.jndi=${datasource.jndi}|
                                cargo.datasource.username=${h2.user}|
                                cargo.datasource.password=${h2.user}</cargo.datasource.datasource.h2>
						</properties>
					</configuration>
					<deployables>
						<deployable>
							<properties>
								<context>addressBook</context>
							</properties>
							<pingURL>http://localhost:9090/addressBook/rest/hello</pingURL>
							<pingTimeout>36000</pingTimeout>
						</deployable>
					</deployables>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<version>2.17</version>
				<executions>
					<execution>
						<id>integration-test</id>
						<goals>
							<goal>integration-test</goal>
						</goals>
					</execution>
					<execution>
						<id>verify</id>
						<goals>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<includes>
						<include>**/*.java</include>
					</includes>
					<groups>com.learner.persistence.harness.Integration</groups>
					<excludedGroups>com.learner.persistence.harness.PreIntegration</excludedGroups>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>2.17</version>
				<configuration>
					<includes>
						<include>**/*.java</include>
					</includes>
					<groups>com.learner.persistence.harness.PreIntegration</groups>
					<excludedGroups>com.learner.persistence.harness.Integration</excludedGroups>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
